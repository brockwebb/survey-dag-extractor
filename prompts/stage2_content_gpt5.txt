# Stage 2: Single-question content extraction (GPT-5 tuned)

Inputs
- qid: canonical question id (e.g., D11, Q3a, FD6_rev, NDX2_OTHER)
- slice: focused text slice containing the full question prompt and, if present, response options/codes.

Task
- Extract the question content in strict JSON.

Output (STRICT JSON)
{
  "id": "STRING",
  "text": "STRING",
  "response_type": "enum|set|text|number|numeric|scale|date|null",
  "response_options": [
    {"code": "STRING|NUMBER", "text": "STRING"}
  ] | null,
  "domain": {
    "min": NUMBER | null,
    "max": NUMBER | null,
    "values": [STRING|NUMBER] | null
  } | null,
  "universe": "STRING|null",
  "universe_ast": ["AST"...] | null
}

Rules
- Quote OPTION TEXT EXACTLY when visible; include numeric/letter codes as shown.
- If options are not visible, set response_options = null (do NOT invent).
- Prefer response_type = "set" for multi-select, "enum" for single-select, "text" for open text, "number|numeric" for numeric entry.
- If numeric constraints are visible (e.g., 0–10), populate domain.min/domain.max.
- If inline skip/universe is visible, set universe text and a simple AST where possible (e.g., ["EQ","Q5",2], ["INCLUDES","NDX2",8], ["TRUE"]).
- Keep JSON minimal, no commentary.

Return ONLY the JSON object above. No commentary.

